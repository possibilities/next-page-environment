{
  "name": "next-page-environment",
  "description": "A decorator that injects server side values across Next.js pages",
  "version": "0.1.0",
  "repository": {
    "url": "https://github.com/possibilities/next-page-environment",
    "type": "git"
  },
  "author": "Mike Bannister <mikebannister@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": "6.x.x"
  },
  "scripts": {
    "dev": "rimraf lib && babel src --out-dir lib --watch",
    "build": "rimraf lib && babel src --out-dir lib",
    "prepublish": "rimraf lib && babel src --out-dir lib",
    "test": "echo TODO",
    "lint": "standard",
    "pretest": "standard"
  },
  "main": "lib/decorators/PageEnvironment.js",
  "files": [
    "lib"
  ],
  "devDependencies": {
    "babel-cli": "^6.24.1",
    "babel-eslint": "^7.1.1",
    "babel-preset-latest": "^6.24.1",
    "next": "^2.1.1",
    "nodemon": "^1.11.0",
    "react": "^15.5.3",
    "react-dom": "^15.5.3",
    "rimraf": "^2.6.1",
    "standard": "^10.0.1"
  },
  "peerDependencies": {
    "next": ">=2.1.1"
  },
  "babel": {
    "presets": [
      "next/babel",
      "latest"
    ]
  },
  "standard": {
    "parser": "babel-eslint"
  },
  "dependencies": {
    "next-page-decorator-invariant": "^0.1.1"
  }
}
